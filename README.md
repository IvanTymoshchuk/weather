Додаток для отримання прогнозу погоди на 7 днів

Щоб запустити код, потрібно встановити бібліотеки `tkinter`, `requests`, `geocoder` та `PIL`. За допомогою цих комманд:

```
pip install tk
```

```
pip install requests
```

```
pip install geocoder
```

```
pip install Pillow
```